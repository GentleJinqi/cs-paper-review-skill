# Revised synthetic version

F-1 now states the threshold. F-2 narrows, but does not fully bound, the
generalisation claim. F-3 still lacks uncertainty. F-4 now reverses the
caption's metric direction and is worse. A new data filter introduces F-5,
which uses test labels.
