# Synthetic reproducibility statement

The method rejects examples when a score exceeds a threshold, but the paper
never gives the threshold or a rule for selecting it. Hardware case colour
and editor theme are not part of the algorithm. Seeds, dataset version, and
the reported compute budget are stated.
