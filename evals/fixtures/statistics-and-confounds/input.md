# Synthetic comparison

Ten patients each contribute 100 correlated images. The paper treats all
1,000 images as independent observations in a two-sample test and splits
images, rather than patients, between train and test. A separate paired
comparison uses one aggregate value per patient and is correctly controlled.
